import 'package:flutter/material.dart';

class FindHotels extends StatelessWidget {
  const FindHotels({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("find hotels"),
    );
  }
}
